# Optional Supabase Postgres connection string
DATABASE_URL=

# Comma-separated Expo web development and deployed preview origins
ALLOWED_ORIGINS=

# Optional Mapbox Directions token. When unset, POST /api/v1/trips uses a mock polyline.
MAPBOX_ACCESS_TOKEN=
