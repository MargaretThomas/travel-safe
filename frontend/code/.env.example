# Android emulators can use http://10.0.2.2:8000 instead.
# Expo Go on a physical device cannot reach a laptop through localhost.
# Replace this with the laptop's LAN address, for example http://192.168.x.x:8000.
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
